<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= _("Title") %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= _("Description") %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= _("Parent Category ID") %><br />
    <%= f.collection_select(:parent_category_id,@parent_categories,:id,:title,{:prompt => _('Select a Category')}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
